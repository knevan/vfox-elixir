name: Generate and Publish Manifest

on:
  push:
    tags: [ 'v*' ]
  pull_request:
    types: [closed]
    branches: [main, master]

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' || (github.event.pull_request.merged == true && startsWith(github.event.pull_request.title, 'Release v'))
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Extract version from tag
        if: github.event_name == 'push'
        run: |
          VERSION=$(echo ${{ github.ref }} | cut -d'/' -f3 | cut -c2-)
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Extract version from PR title
        if: github.event_name == 'pull_request'
        run: |
          PR_TITLE="${{ github.event.pull_request.title }}"
          VERSION=$(echo "$PR_TITLE" | grep -oP 'Release v\K[0-9]+\.[0-9]+\.[0-9]+')
          echo "VERSION=$VERSION" >> $GITHUB_ENV
      
      - name: Check metadata.lua version
        run: |
          METADATA_VERSION=$(grep -oP 'PLUGIN.version = "\K[^"]+' metadata.lua)
          echo "Tag Version: $VERSION"
          echo "Metadata Version: $METADATA_VERSION"
          
          if [ "$VERSION" != "$METADATA_VERSION" ]; then
            echo "::error::Version mismatch detected!"
            echo "::error::Git Tag is v$VERSION, but metadata.lua says $METADATA_VERSION"
            echo "::error::Please update PLUGIN.version in metadata.lua to match the tag."
            exit 1
          fi
          echo "Version consistency check passed."

      - name: Validate version
        run: |
          if [[ ! "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Invalid version format: $VERSION"
            exit 1
          fi
          echo "Publishing version: $VERSION"

      - name: Publish manifest and release
        uses: version-fox/plugin-manifest-action@main
        with:
          version: ${{ env.VERSION }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
